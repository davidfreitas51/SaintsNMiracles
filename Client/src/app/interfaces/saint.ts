export interface Saint {
    id: number
    name: string
    country: string
    century: number
    image: string
    description: string
    markdownContent: string
    slug: string
    createdAt: string
}